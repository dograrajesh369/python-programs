# Write a program to find the sum
# of first n natural numbers using whileloop

n=0
while(sum==n):
     n = int(input("Enter your number :- "))
for i in range(1, n+1):
        sum = n+i
        print(f"The sum of first 'n' natural number is {sum}") 